# Runtime dependencies for Telegram Exporter.
# Versions pinned by major/minor for reproducibility and protection
# against breakage from major upgrades. Patch-security updates
# are picked up automatically within the range.

# Telegram API client
telethon>=1.36,<2.0

# UI framework
customtkinter>=5.2,<6.0

# System secrets storage
keyring>=24.0,<26.0

# ffmpeg for audio conversion
imageio-ffmpeg>=0.4.9,<1.0

# SOCKS proxy for telethon (optional, but build expects it)
PySocks>=1.7,<2.0

# Local Whisper transcription
faster-whisper>=1.0,<2.0

# Deepgram uses stdlib urllib, no extra deps required.

# Calendar popup widget for date pickers in export and folder filters.
# NOTE: tkcalendar is GPLv3-licensed; combined distribution would have to
# follow GPL terms. The application code itself stays MIT, but downstream
# packagers should be aware of the implication.
tkcalendar>=1.6,<2.0
