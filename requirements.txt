# Runtime dependencies for Telegram Exporter.
# Versions pinned by major/minor for reproducibility and protection
# against breakage from major upgrades. Patch-security updates
# are picked up automatically within the range.

# Telegram API client
telethon>=1.36,<2.0

# UI framework
customtkinter>=5.2,<6.0

# System secrets storage
keyring>=24.0,<26.0

# ffmpeg for audio conversion
imageio-ffmpeg>=0.4.9,<1.0

# Proxy support for telethon (per-account SOCKS5/HTTP/MTProto).
# Telethon 1.36+ prefers python-socks and IGNORES the proxy (with only a
# UserWarning) if it is absent — PySocks alone is NOT enough for socks5/http.
# The [asyncio] extra is required because telethon connects via
# python_socks.async_.asyncio.Proxy. PySocks kept as a legacy fallback.
python-socks[asyncio]>=2.0,<3.0
PySocks>=1.7,<2.0

# Local Whisper transcription
faster-whisper>=1.0,<2.0

# Deepgram uses stdlib urllib, no extra deps required.
